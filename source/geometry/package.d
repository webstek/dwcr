module geometry;

import geometry.mesh;  // laods mesh data from obj files and does mesh related computations
import geometry.model; // represents an object made from a mesh and a material
import geometry.scene; // holds all the models that make up a scene